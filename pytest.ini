[pytest]
# Scope the project's own suite to tests/. The data/ tree holds standalone
# exercise & sandbox fixtures (they expect their own CWD / sys.path and error
# at collection), so they must not be swept up by a bare `pytest` invocation.
testpaths = tests
